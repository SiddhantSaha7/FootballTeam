package controller;

/**
 * Interface representing the controller for the U10 soccer application.
 * Starts the app and initializes view interaction.
 */
public interface ISoccerTeamController {

    /**
     * Starts the controller by initializing the view and wiring listeners.
     */
    void startController();
}